{
    "builds": [{
        "src": "app/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi@latest",
        "config": { "maxLambdaSize": "15mb","runtime":"python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "app/wsgi.py"
        }
    ]
}